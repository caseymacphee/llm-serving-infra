# cp values-secrets.example.yaml values-secrets.yaml  (gitignored)
# Generate each with: openssl rand -hex 32
langfuse:
  salt:
    value: ""
  encryptionKey:
    value: ""
  nextauth:
    secret:
      value: ""

postgresql:
  auth:
    password: ""

clickhouse:
  auth:
    password: ""

redis:
  auth:
    password: ""

s3:
  auth:
    rootPassword: ""
